# Chess game

## How to build

This project requires a C++ compiler supporting at least C++11.

Create a build directory and go in it:

```
mkdir build && cd build
```

Generate the build files:
```
cmake ..
```

Compile the project:
```
make
```

Run the game:
```
./echecs
```

Now you can play !